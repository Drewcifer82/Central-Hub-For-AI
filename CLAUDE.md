# Central Hub For AI — Project Rules

## Git
Always Ask Permission To Commit And Push.
Andrew and Claude work as a team. Discuss changes first, then ask before running any `git commit` or `git push`.
